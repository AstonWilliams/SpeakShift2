create a page named batch(for batch transcriptions), and add it in sidebar.tsx, uses same UI as other pages, and it has drop container, just like transcribe page, and accepts zips,drive links,folders(Dropped or manually selection using tauri native), a queue is created , it can be renamed, queue processing config for each queue is set, default is one item per queue, user can choose to set sequence of queues,selects model(Whisper),selects export format(srt,vtt,json,pdf,docx,txt,etc),selects output location, output folders are created at that location with name of queue and srts, or exports are created there, user can choose either to tweak global config for all queues,or set custom config for each queuecreate a page named batch(for batch transcriptions), and add it in sidebar.tsx, uses same UI as other pages, and it has drop container, just like transcribe page, and accepts zips,drive links,folders(Dropped or manually selection using tauri native), a queue is created , it can be renamed, queue processing config for each queue is set, default is one item per queue, user can choose to set sequence of queues,selects model(Whisper),selects export format(srt,vtt,json,pdf,docx,txt,etc),selects output location, output folders are created at that location with name of queue and srts, or exports are created there, user can choose either to tweak global config for all queues,or set custom config for each queue